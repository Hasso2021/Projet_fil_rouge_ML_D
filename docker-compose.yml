version: '3.8'

services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - SD_DEVICE=cpu
      - API_HOST=0.0.0.0
      - API_PORT=8000
    volumes:
      - ./outputs:/app/outputs
      - ./models:/app/models
    restart: unless-stopped

